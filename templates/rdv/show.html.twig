{% extends 'base.html.twig' %}

{% block title %}Rdv
{% endblock %}

{% block section %}
	<section class="mypage"></section>

	<h1>Rdv</h1>

	<table class="table">
		<tbody>
			<div class="card mycardpublication" style="width: 18rem;">

				<tr>
					<th>Id</th>
					<td>{{ rdv.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ rdv.nom }}</td>
				</tr>
				<tr>
					<th>Date</th>
					<td>{{ rdv.Date ? rdv.Date|date('Y-m-d') : '' }}</td>
				</tr>
				<tr>
					<th>Nom Medecin</th>
					<td>{{ rdv.medecin}}</td>
				</tr>
			</div>
		</tbody>
	</tbody>
</table>

<a href="{{ path('rdv_index') }}">back to list</a>

<a href="{{ path('rdv_edit', {'id': rdv.id}) }}">edit</a>

{{ include('rdv/_delete_form.html.twig') }}{% endblock %}
