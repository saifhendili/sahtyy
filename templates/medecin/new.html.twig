{% extends 'base.html.twig' %}

{% block title %}New Medecin{% endblock %}

{% block section %}
    <section class="mypage"></section>
    <h1>Create new Medecin</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('medecin_index') }}">back to list</a>
{% endblock %}
