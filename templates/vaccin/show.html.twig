{% extends 'base.html.twig' %}

{% block title %}Vaccin{% endblock %}

{% block section %}
<section class="mypage"></section>
<div class="head-mybody"> 
    <h1>Vaccin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaccin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ vaccin.nom }}</td>
            </tr>
      
            <tr>
                <th>Description</th>
                <td>{{ vaccin.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ vaccin.prix }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ vaccin.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vaccin_index') }}">back to list</a>

    <a href="{{ path('vaccin_edit', {'id': vaccin.id}) }}">edit</a>

    {{ include('vaccin/_delete_form.html.twig') }}
     <a href="{{ path('vaccin_show', {'id': vaccin.id}) }}">go to vaccin</a>
    </div>
{% endblock %}
