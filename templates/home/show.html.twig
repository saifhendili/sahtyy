{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block section %}
<section class="mypage"></section>
    <h1>Home</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ home.title }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ home.img }}</td>
            </tr>
            <tr>
                <th>Img2</th>
                <td>{{ home.img2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_index') }}">back to list</a>

    <a href="{{ path('home_edit', {'id': home.id}) }}">edit</a>

    {{ include('home/_delete_form.html.twig') }}
{% endblock %}
