{% extends 'base.html.twig' %}

{% block title %}FormAide{% endblock %}

{% block section %}
<section class="mypage"></section>
    <h1>FormAide</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_aide.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ form_aide.IdUser }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ form_aide.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ form_aide.prenom }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ form_aide.img }}</td>
            </tr>
            <tr>
                <th>Textpub</th>
                <td>{{ form_aide.textpub }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_aide_index') }}">back to list</a>

    <a href="{{ path('form_aide_edit', {'id': form_aide.id}) }}">edit</a>

    {{ include('form_aide/_delete_form.html.twig') }}
{% endblock %}
