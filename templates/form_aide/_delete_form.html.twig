<form method="post" action="{{ path('form_aide_delete', {'id': form_aide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_aide.id) }}">
    <button class="btn">Delete</button>
</form>
