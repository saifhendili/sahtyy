<form method="post" action="{{ path('reservation_med_delete', {'id': reservation_med.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_med.id) }}">
    <button class="btn">Delete</button>
</form>
